#include "../minishell.h"

// char **tokenizer(char *input)
// {
//         char **tokens;

        
//         return (tokens);    
// }

t_command *parser(char *input)
{
        t_command *cmd = NULL;
        // char **tokens;
        (void)input;
        // tokens = tokenizer(input);
        printf("parser\n");
        return cmd;
}