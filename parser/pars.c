#include "../minishell.h"

char **tokenizer(char *input)
{
        char **tokens;

        
        return (tokens);    
}

void parser(char *input)
{
        char **tokens;

        tokens = tokenizer(input);
}